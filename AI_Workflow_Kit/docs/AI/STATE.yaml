# Live orchestration state — Orchestrator owns this file.
# Keep it simple. Project-agnostic template.

project_prefix: vaniscript-as
onboarding:
  status: complete
  model_pairs_confirmed: true
  completed_at: 2026-08-11T05:55:29Z
current_step: S1
# Exact STEPS.md Do text while one item is actively assigned; Main owns/clears it.
current_work_item: null
step_description: >-
  Port the BOLABOL Parakeet Core ML engine and robust 16 kHz mono preparation
  into VaniScript without wiring Canary or the product pipeline yet.
target_files:
  - Sources/VaniScript/Services/LocalASREngine.swift
  - Sources/VaniScript/Services/LocalASRAudioPreprocessor.swift
  - Sources/VaniScript/Services/ParakeetTranscriptionEngine.swift
  - Tests/VaniScriptTests/LocalASRAudioPreprocessorTests.swift
  - Tests/VaniScriptTests/ParakeetTranscriptionEngineTests.swift
  - Tests/VaniScriptCoreTests/NativeModelCatalogTests.swift
  - Tests/VaniScriptTests/ModelDownloadManagerTests.swift
completed_steps:
  - S0
  - S1
skipped_steps: []
implementation:
  # pending | running | waiting_review | changes_requested | blocked | complete
  status: complete
  attempts: 1
  # Increment only after evidence evaluates an implementation attempt.
  # Runtime/provider interruption alone is not an implementation attempt.
retry_guard:
  max_attempts_without_progress: 3
  # Consecutive verified failures for the current approach/failure signature.
  # First occurrence = 1; same approach + same failure increments; a new
  # approach/evidence/material failure signature starts a new count at 1.
  repeated_failure_count: 0
  last_failure_signature: null
  blocker: null
omp:
  active_agent: null
  active_role: null
  last_result_artifact: agent://LASR03ParakeetQA2
  model_failure:
    # none | awaiting_human | backup_authorized
    status: none
    role: null
    primary_agent: null
    failed_model: null
    evidence: null
    backup_agent: null
    human_instruction: null
    authorized_at: null
  interruption:
    # none | interrupted_no_changes | interrupted_partial | recovered_result | indeterminate
    status: recovered_result
    role: tester
    agent: LASR03ParakeetQA2
    changed_files:
      - Tests/VaniScriptTests/LocalASRAudioPreprocessorTests.swift
      - Tests/VaniScriptTests/ParakeetTranscriptionEngineTests.swift
    evidence: >-
      The original Tester primary failed before execution with provider HTTP
      404. The Human selected google-antigravity/gemini-3.6-flash:high as the
      new primary. It completed targeted QA; Main inspected both added boundary
      tests and independently verified 9 focused tests in 2 suites with 0
      failures.
    recorded_at: 2026-08-11T06:57:11Z
review:
  status: approved
  last_feedback_file: AI_Workflow_Kit/docs/AI/FEEDBACK.md
  verdict: APPROVED
  note: Human-authorized Reviewer backup approved both S1 Judgment Gates.
  # default on unless Human explicitly skips
  enabled: true
  # use status: skipped only after an explicit Human opt-out; record why in note
qa:
  status: qa_green
  last_report: AI_Workflow_Kit/docs/AI/REPORT.md
  suite: "swift test --filter 'LocalASRAudioPreprocessorTests|ParakeetTranscriptionEngineTests'"
  bugs_open: 0
  note: Two deterministic boundary tests added; focused gate passed 9/9.
  # recommended on; Human may opt out
  enabled: true
  # use status: skipped only after an explicit Human opt-out; record why in note
security:
  next_run: none
  note: >-
    Offer once near release when work is ready (optional). Top models only:
    GLM 5.2 max, Sol max, or Opus 5 max. Not every step. Not Terra/Luna.
next_actor: human
track: LOCAL_ASR_COREML
plan_files:
  - PIPELINE.md
  - AI_Workflow_Kit/docs/STEPS.md
  - AI_Workflow_Kit/docs/DECISIONS.md
  - AI_Workflow_Kit/docs/PROJECT_CONTEXT.md
checkpoint:
  last_pre_tag: vaniscript-as/pre-S1
  last_post_tag: null
  last_commit: 37b649e
coder_brief: |
  S1 only: port the BOLABOL Parakeet engine, minimal app-target engine contract,
  and robust audio preprocessor. Preserve current catalog/download work. Do not
  wire Canary, NativeProcessingPipeline, Models UI, or cloud download behavior.
notes: |
  Human requested all three BOLABOL Core ML engines. Existing LASR-01 catalog
  and LASR-02 download/presence work is preserved. Canary 1B cloud metadata is
  intentionally deferred; current scope assumes a verified local package.
  No Git remote is configured for the workspace repository, so the pre-S1
  checkpoint is local only.
  Human requested a pause after S1 Tester completion so the workflow can be
  updated before any further product work.
